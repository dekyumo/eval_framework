# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: e2e.spec.ts >> E2E Eval Framework Flow >> Complete flow: scan -> dataset -> case -> run -> score -> campaign -> human eval
- Location: tests\e2e.spec.ts:5:3

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://127.0.0.1:5000/
Call log:
  - navigating to "http://127.0.0.1:5000/", waiting until "load"

```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | 
  3   | test.describe('E2E Eval Framework Flow', () => {
  4   |   // Use a slow mo or standard setup if needed
  5   |   test('Complete flow: scan -> dataset -> case -> run -> score -> campaign -> human eval', async ({ page }) => {
  6   |     
  7   |     // Step 1: Scan the agent
> 8   |     await page.goto('/');
      |                ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://127.0.0.1:5000/
  9   |     await expect(page.getByRole('heading', { name: 'Add target & scan' })).toBeVisible();
  10  |     
  11  |     // Check if the repo path is set (it should be set by the API)
  12  |     await expect(page.locator('input[placeholder="Loading repository path..."]')).not.toHaveValue('');
  13  |     
  14  |     // Input Agent Path and Commit
  15  |     await page.getByPlaceholder('src.agent:my_agent').fill('a_single_agent.day_trip:root_agent');
  16  |     await page.getByRole('button', { name: 'Scan Agent' }).click();
  17  |     
  18  |     // We should be navigated to the Agents page
  19  |     await expect(page.url()).toContain('/agents');
  20  |     await expect(page.getByRole('heading', { name: 'Agents Graph & Lineage' })).toBeVisible();
  21  |     
  22  |     // Select the newly scanned snapshot
  23  |     await page.locator('select').selectOption({ index: 1 }); // Assuming it's the first one in the list
  24  |     
  25  |     // Update Domain
  26  |     await page.getByLabel('In Domain').fill('a day in <SOME TOWN> with a budget of $n.nn');
  27  |     await page.getByLabel('Out of Domain').fill('everything else');
  28  |     await page.getByRole('button', { name: 'Save Domain' }).click();
  29  | 
  30  |     // Step 2: Registries - Create Dataset, Tags, Rubrics, Extractors
  31  |     await page.getByRole('link', { name: 'Registries' }).click();
  32  |     await expect(page.url()).toContain('/registries');
  33  |     
  34  |     // Create Extractor
  35  |     await page.getByRole('button', { name: 'extractors' }).click();
  36  |     await page.getByPlaceholder('e.g. get_refund_amount').fill('extract_budget');
  37  |     await page.getByRole('combobox').selectOption('float');
  38  |     await page.locator('textarea').fill('def extract(trace):\n    # dummy implementation\n    return 100.00');
  39  |     await page.getByRole('button', { name: 'Save Extractor' }).click();
  40  | 
  41  |     // Create Rubric
  42  |     await page.getByRole('button', { name: 'rubrics' }).click();
  43  |     // Assuming UI for rubric creation exists, this would be:
  44  |     // await page.getByPlaceholder('Rubric Name').fill('Helpfulness');
  45  |     // await page.getByRole('button', { name: 'Save Rubric' }).click();
  46  | 
  47  |     // Create Tag
  48  |     await page.getByRole('button', { name: 'tags' }).click();
  49  |     // await page.getByPlaceholder('Tag Name').fill('budget-test');
  50  |     // await page.getByRole('button', { name: 'Save Tag' }).click();
  51  | 
  52  |     // Create Dataset
  53  |     await page.getByRole('button', { name: 'datasets' }).click();
  54  |     // await page.getByPlaceholder('Dataset Name').fill('DayTrip Tests');
  55  |     // await page.getByRole('button', { name: 'Save Dataset' }).click();
  56  | 
  57  |     // Step 3: Create Case
  58  |     await page.getByRole('link', { name: 'Cases & Evals' }).click();
  59  |     await expect(page.url()).toContain('/cases');
  60  |     
  61  |     // Fill out the case builder
  62  |     await page.getByLabel('Case Name').fill('Trip to Paris $500');
  63  |     // await page.getByLabel('Dataset').selectOption('DayTrip Tests');
  64  |     await page.getByLabel('Domain Position').selectOption('in');
  65  |     await page.getByLabel('Problem Type').selectOption('happy');
  66  |     await page.getByLabel('Split').selectOption('judging');
  67  |     
  68  |     // Build conversation
  69  |     await page.locator('textarea').fill('Plan a day in Paris with a budget of $500.00');
  70  |     await page.getByRole('button', { name: 'Save Case' }).click();
  71  | 
  72  |     // Step 4: Run Generation
  73  |     await page.getByRole('link', { name: 'Run Generation' }).click();
  74  |     await expect(page.url()).toContain('/runs');
  75  |     
  76  |     // Select Snapshot and Dataset
  77  |     await page.locator('select').nth(0).selectOption({ index: 1 }); // Snapshot
  78  |     // await page.locator('select').nth(1).selectOption('DayTrip Tests'); // Dataset
  79  |     await page.getByRole('button', { name: 'Generate Traces' }).click();
  80  |     
  81  |     // Wait for the run to appear
  82  |     await expect(page.locator('.text-indigo-900').first()).toBeVisible({ timeout: 10000 });
  83  |     await page.locator('.text-indigo-900').first().click(); // View the trace
  84  |     
  85  |     // Step 5: Run Evals
  86  |     await page.getByRole('link', { name: 'Run Evals' }).click();
  87  |     await expect(page.url()).toContain('/evals');
  88  |     
  89  |     await page.locator('select').nth(0).selectOption({ index: 1 }); // Snapshot
  90  |     // await page.locator('select').nth(1).selectOption('DayTrip Tests'); // Dataset
  91  |     await page.getByRole('button', { name: 'Run Evaluations' }).click();
  92  | 
  93  |     // Wait for scored run
  94  |     await expect(page.locator('.text-slate-800').first()).toBeVisible({ timeout: 10000 });
  95  | 
  96  |     // Step 6: Campaigns
  97  |     await page.getByRole('link', { name: 'Campaigns' }).click();
  98  |     await expect(page.url()).toContain('/campaigns');
  99  |     
  100 |     await page.getByPlaceholder('e.g. Model Size Eval').fill('Model Comparison Campaign');
  101 |     await page.getByLabel('Models (comma-separated)').fill('gemini-2.5-flash, gemini-2.5-flash-lite, gemini-3.1-pro-preview');
  102 |     await page.getByRole('button', { name: 'Launch' }).click();
  103 | 
  104 |     // Step 7: Human Eval
  105 |     await page.getByRole('link', { name: 'Human Eval' }).click();
  106 |     await expect(page.url()).toContain('/human-eval');
  107 |     
  108 |     await page.locator('select').nth(0).selectOption({ index: 1 }); // Select a trace
```