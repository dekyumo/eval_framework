A simple agent that will take the agent snapshot (a full description of the agent), the agent distribution (what's chats are in distribution, out of distribution, at the margin of distribution for the agent), what the user wants (in distribution, out of distribution, at the margin) plus the type of problem (happy path, adversarial request, client related problems, technical problems), set the sampling temperature to high, and produce a trace.