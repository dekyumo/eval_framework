- pointing the evaluation framework on the agent to be evaluated is difficult:
    - set the AGENT_ENTRYPOINT variable
    - has to follow a naming convention
    - this is documented in the source code of the app
    - setting the AGENT_ENTRYPOINT doesn't change where the evaluation framework think the DB is (the DB path is also documented in the code)
- the web interface has been underspecified:
    - given an agent with the above steps, there is no button to scan the agent
    - the Agents, Cases, Runs menu items return a JSON view and not an HTML page
    - when pointing the framework to some agent with the above steps, the Agents route does not update its JSON
    - since the chat agent has been specced first, it is the only agent available, because it has the most basic functionality. But it is not possible to run the evaluation framework without an agent scanner 
    - this prevents dogfooding the eval framework
    - getting used to React and SPAs, the simple flask app now seems ugly and clunky. But the simple flask pages are in the spec, so this is a spec problem.
- reading the code:
    - the eval framework is centered on the google ADK, yet there is a compatibility shim to use it with other frameworks. This is not useful, and has not been requested. The whole assumption of "agents are a directory" comes from the ADK itself. No idea how this could be hallucinated.
    - ADK not in requirements (ran pip install google-adk==2.3.0)
    - the sub agents for the chat operator have been mocked (not sure if this was in the spec, maybe OK)
    - all the tools are available from v0 ! great
    - the clustering was supposed to be an interface (because many implementations have been described in SOUL2), but this is just a python function
    - the "detect_regression" is a full page of code to detect if something changes above a threshold. This is an UI problem (track a metric on a git tree), not a code problem (see if something changes by n percentage points), and it probably shouldn't be a full page of code
    - IRT is implemented on first gen ! great
    - the agent_runner.py has been mocked for some reason. But real agent running code is "runner, session = create_adk_runner_and_session(app_name), for event in agent.run(session, message): #evaluate message parts and events here", so I'm not sure why this was mocked. Mocking eval runs was in the spec ? Composer is unfamiliar with ADK ?
    - in exec_script.py, the agent path is hard coded again ("agent.py:root_agent"), again documented in the code. Not sure what exec_script.py does either, it seems to duplicate some framework functionality in a self contained script. The reason for it is unknown.
    - introspect_script.py: there is an agent introspector ! after a bit of command line test (python -m eval_workbench.runner.introspect_script cuppibla_example_agents.a_single_agent.day_trip root_agent), it returns a JSON block (not sure how to use it). Not sure how to specify where the limits of its Git repo is. Not sure why it is not accessible from the webapp
    - git worktree isolation implementation is 400 lines of code ! Made with a cheap model ! It would probably be better to use a known library, this is a brittle part of the implem.
    - there is an agent scanner ! Not sure how it is wired into the general framework or how to activate it.

TLDR: the main problems after the first version are:
- the assumption is made everywhere that there is one agent per git repo. This is not the case. The kuzu DB is put in the git root, and becomes shared. This might be correct (if we want to separately eval the subagents and the main agent)
- what happens if there are several agents in one git repo, and how to separately test subagents is underspecified
- the chat agent has been specified first, whereas the scanner agent would have enabled dog fooding the eval framework and was more important. Also, not sure the implementing agent understand that the scanner is an ADK agent
- because the scanner is a scaffold, there is no way to specify the domain for the agent. It was supposed to be detected and modifiable per snapshot, but it is just missing.
- the core spec specifies intent[] for all agents, but this is not the case. intent[] is for routing agents and supposes a closed set (enum) of intents that can be tested with an F1 score. This is not true for all agents, and is part of the metrics, not a core feature of an eval case.
- the site has been underspecified, most menus lead into pages with no buttons or raw JSON, except the "create eval" page that was fully specified
- no way to directly specify a git root, an agent root within that git root, and then press a button to scan an agent and start generating eval traces.
- specifying only one of the 4 agents was a mistake. Without the 4 agents, the evaluation framework cannot be used fully, which prevents dog fooding and using it to test the 4 agents. (Incidently, those 4 agents need to be in a git submodule so that the evaluation framework can switch versions, or just git worktree the whole evaluation framework)
- creating and editing tags has not been specified (website underspecified again). The tag set should be closed.
- editing eval cases has not been specified (the generation agent is a stub, and then there's no edit page) (website underspecified again)
- editing metrics (no route, no site)
- editing rubrics (no route, no site)
- editing extraction function (no route, no site)
